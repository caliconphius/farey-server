services:
  backend:
    build:
      context: backend
      dockerfile: Dockerfile
      target: runtime
    container_name: backend
    environment:
      PORT: "8080"
    restart: unless-stopped
  proxy:
    build: proxy
    container_name: proxy
    ports:
      - 80:80
      - 8090:8090
    depends_on:
      - backend
